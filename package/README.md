# To build the"eggsecutable" egg

Pull `sas` from https://github/uwbmrb/SAS

Pull `starobj` from https://github/uwbmrb/starobj

Edit `setup.py` and change paths to the above

Run `python setup.py bdist_egg`
